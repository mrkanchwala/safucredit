[pytest]
# A globally installed anchorpy registers a pytest plugin whose own dependency is broken on the dev Mac, and it
# crashes collection before any test runs. Nothing here uses it; disabling it by name keeps bare `pytest` working
# without touching the global Python install.
addopts = -p no:pytest_anchorpy
testpaths = verdict/tests scripts/tests
