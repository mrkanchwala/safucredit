[package]
name = "stock_vault"
version = "0.1.0"
description = "SAFU Credit: USDC loans against tokenized stocks, with wrongful-liquidation guards"
license.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "lib"]
name = "stock_vault"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = "=1.1.2"
anchor-spl = { version = "=1.1.2", features = ["token_2022", "token_2022_extensions"] }
# Exact pin (U6): a safu-core change must be adopted here on purpose, never picked up silently.
safu-core = { path = "../../../crates/safu-core", version = "=0.2.0" }

[dev-dependencies]
litesvm = { version = "0.10.0", features = ["precompiles"] }
solana-message = "3.0.1"
solana-transaction = "3.0.2"
solana-signer = "3.0.0"
solana-keypair = "3.0.1"
spl-token-2022-interface = "=2.1.0"
spl-token-interface = "=2.0.0"
solana-system-interface = { version = "2.0.0", features = ["bincode"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
