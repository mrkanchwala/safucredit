/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const POSITION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  170, 188, 143, 228, 122, 64, 247, 208,
]);

export function getPositionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(POSITION_DISCRIMINATOR);
}

export type Position = {
  discriminator: ReadonlyUint8Array;
  version: number;
  bump: number;
  market: Address;
  owner: Address;
  rawCollateral: bigint;
  debtShares: bigint;
  /** Coverage in force when this loan was opened (U3: later changes never shrink an open loan's cover). */
  coverageBps: number;
  /**
   * Debt-weighted borrow time (unix seconds), 0 while there is no debt. Each borrow pulls it toward
   * now in proportion to its size; full repayment resets it. The backstop's 60-day gate reads it, so
   * a tiny early loan topped up after a price breaks still counts as young.
   */
  borrowAgeTs: bigint;
  /**
   * Where a wrongful-liquidation payback goes. Defaults to the owner, who may change it. A liquidation
   * record freezes it, so nobody can redirect a payback after the fact.
   */
  payout: Address;
  /**
   * When this position was first seen liquidatable by `mark_liquidatable`, and most recently. 0 = not
   * marked. Outside liquidators wait on these; the pool does not.
   */
  liquidatableFirstSeen: bigint;
  liquidatableLastSeen: bigint;
  /**
   * Set by `write_down_collateral` once any of this position's collateral is attributed to an issuer seizure.
   * Permanent. Every later liquidation of this position records `issuer_halt = true`, so the backstop refuses
   * a payback claim on it, and any debt it leaves behind is issuer loss, never reimbursable bad debt.
   */
  issuerSeized: boolean;
  /** Raw collateral ever written down on this position. */
  seizedRawTotal: bigint;
  /** Taken from the old 32 reserved bytes (8 + 8 + 1 + 8). */
  reserved: ReadonlyUint8Array;
};

export type PositionArgs = {
  version: number;
  bump: number;
  market: Address;
  owner: Address;
  rawCollateral: number | bigint;
  debtShares: number | bigint;
  /** Coverage in force when this loan was opened (U3: later changes never shrink an open loan's cover). */
  coverageBps: number;
  /**
   * Debt-weighted borrow time (unix seconds), 0 while there is no debt. Each borrow pulls it toward
   * now in proportion to its size; full repayment resets it. The backstop's 60-day gate reads it, so
   * a tiny early loan topped up after a price breaks still counts as young.
   */
  borrowAgeTs: number | bigint;
  /**
   * Where a wrongful-liquidation payback goes. Defaults to the owner, who may change it. A liquidation
   * record freezes it, so nobody can redirect a payback after the fact.
   */
  payout: Address;
  /**
   * When this position was first seen liquidatable by `mark_liquidatable`, and most recently. 0 = not
   * marked. Outside liquidators wait on these; the pool does not.
   */
  liquidatableFirstSeen: number | bigint;
  liquidatableLastSeen: number | bigint;
  /**
   * Set by `write_down_collateral` once any of this position's collateral is attributed to an issuer seizure.
   * Permanent. Every later liquidation of this position records `issuer_halt = true`, so the backstop refuses
   * a payback claim on it, and any debt it leaves behind is issuer loss, never reimbursable bad debt.
   */
  issuerSeized: boolean;
  /** Raw collateral ever written down on this position. */
  seizedRawTotal: number | bigint;
  /** Taken from the old 32 reserved bytes (8 + 8 + 1 + 8). */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link PositionArgs} account data. */
export function getPositionEncoder(): FixedSizeEncoder<PositionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["market", getAddressEncoder()],
      ["owner", getAddressEncoder()],
      ["rawCollateral", getU64Encoder()],
      ["debtShares", getU128Encoder()],
      ["coverageBps", getU32Encoder()],
      ["borrowAgeTs", getI64Encoder()],
      ["payout", getAddressEncoder()],
      ["liquidatableFirstSeen", getI64Encoder()],
      ["liquidatableLastSeen", getI64Encoder()],
      ["issuerSeized", getBooleanEncoder()],
      ["seizedRawTotal", getU64Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 7)],
    ]),
    (value) => ({ ...value, discriminator: POSITION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Position} account data. */
export function getPositionDecoder(): FixedSizeDecoder<Position> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["market", getAddressDecoder()],
    ["owner", getAddressDecoder()],
    ["rawCollateral", getU64Decoder()],
    ["debtShares", getU128Decoder()],
    ["coverageBps", getU32Decoder()],
    ["borrowAgeTs", getI64Decoder()],
    ["payout", getAddressDecoder()],
    ["liquidatableFirstSeen", getI64Decoder()],
    ["liquidatableLastSeen", getI64Decoder()],
    ["issuerSeized", getBooleanDecoder()],
    ["seizedRawTotal", getU64Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 7)],
  ]);
}

/** Gets the codec for {@link Position} account data. */
export function getPositionCodec(): FixedSizeCodec<PositionArgs, Position> {
  return combineCodec(getPositionEncoder(), getPositionDecoder());
}

export function decodePosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Position, TAddress>;
export function decodePosition<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Position, TAddress>;
export function decodePosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Position, TAddress> | MaybeAccount<Position, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPositionDecoder(),
  );
}

export async function fetchPosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Position, TAddress>> {
  const maybeAccount = await fetchMaybePosition(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Position, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePosition(maybeAccount);
}

export async function fetchAllPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Position>[]> {
  const maybeAccounts = await fetchAllMaybePosition(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Position>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePosition(maybeAccount));
}

export function getPositionSize(): number {
  return 174;
}
