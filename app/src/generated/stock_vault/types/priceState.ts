/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

export type PriceState = {
  /** Ring buffer, USD per whole share with 8 decimals. */
  prices: Array<bigint>;
  timestamps: Array<bigint>;
  /** Next write position. */
  head: number;
  count: number;
  lastPrice: bigint;
  lastUpdate: bigint;
  lastClose: bigint;
  /** Whether the underlying equity market was open at the last accepted update. */
  marketOpen: boolean;
  /** Set when an update broke the spike cap. Blocks borrowing and liquidation until cleared. */
  flagged: boolean;
  /** The rejected price, so a second consecutive update confirming it can be accepted. */
  flaggedPrice: bigint;
};

export type PriceStateArgs = {
  /** Ring buffer, USD per whole share with 8 decimals. */
  prices: Array<number | bigint>;
  timestamps: Array<number | bigint>;
  /** Next write position. */
  head: number;
  count: number;
  lastPrice: number | bigint;
  lastUpdate: number | bigint;
  lastClose: number | bigint;
  /** Whether the underlying equity market was open at the last accepted update. */
  marketOpen: boolean;
  /** Set when an update broke the spike cap. Blocks borrowing and liquidation until cleared. */
  flagged: boolean;
  /** The rejected price, so a second consecutive update confirming it can be accepted. */
  flaggedPrice: number | bigint;
};

export function getPriceStateEncoder(): FixedSizeEncoder<PriceStateArgs> {
  return getStructEncoder([
    ["prices", getArrayEncoder(getU64Encoder(), { size: 16 })],
    ["timestamps", getArrayEncoder(getI64Encoder(), { size: 16 })],
    ["head", getU8Encoder()],
    ["count", getU8Encoder()],
    ["lastPrice", getU64Encoder()],
    ["lastUpdate", getI64Encoder()],
    ["lastClose", getU64Encoder()],
    ["marketOpen", getBooleanEncoder()],
    ["flagged", getBooleanEncoder()],
    ["flaggedPrice", getU64Encoder()],
  ]);
}

export function getPriceStateDecoder(): FixedSizeDecoder<PriceState> {
  return getStructDecoder([
    ["prices", getArrayDecoder(getU64Decoder(), { size: 16 })],
    ["timestamps", getArrayDecoder(getI64Decoder(), { size: 16 })],
    ["head", getU8Decoder()],
    ["count", getU8Decoder()],
    ["lastPrice", getU64Decoder()],
    ["lastUpdate", getI64Decoder()],
    ["lastClose", getU64Decoder()],
    ["marketOpen", getBooleanDecoder()],
    ["flagged", getBooleanDecoder()],
    ["flaggedPrice", getU64Decoder()],
  ]);
}

export function getPriceStateCodec(): FixedSizeCodec<
  PriceStateArgs,
  PriceState
> {
  return combineCodec(getPriceStateEncoder(), getPriceStateDecoder());
}
