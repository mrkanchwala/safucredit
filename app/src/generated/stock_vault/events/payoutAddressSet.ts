/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const PAYOUT_ADDRESS_SET_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([22, 203, 86, 238, 91, 0, 189, 210]);

export function getPayoutAddressSetEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PAYOUT_ADDRESS_SET_EVENT_DISCRIMINATOR,
  );
}

export type PayoutAddressSetEvent = { position: Address; payout: Address };

export type PayoutAddressSetEventArgs = PayoutAddressSetEvent;

/** Gets the encoder for {@link PayoutAddressSetEventArgs} event data. */
export function getPayoutAddressSetEventEncoder(): FixedSizeEncoder<PayoutAddressSetEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["position", getAddressEncoder()],
      ["payout", getAddressEncoder()],
    ]),
    [getConstantEncoder(PAYOUT_ADDRESS_SET_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link PayoutAddressSetEvent} event data. */
export function getPayoutAddressSetEventDecoder(): FixedSizeDecoder<PayoutAddressSetEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["position", getAddressDecoder()],
      ["payout", getAddressDecoder()],
    ]),
    [getConstantDecoder(PAYOUT_ADDRESS_SET_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link PayoutAddressSetEvent} event data. */
export function getPayoutAddressSetEventCodec(): FixedSizeCodec<
  PayoutAddressSetEventArgs,
  PayoutAddressSetEvent
> {
  return combineCodec(
    getPayoutAddressSetEventEncoder(),
    getPayoutAddressSetEventDecoder(),
  );
}

/** Parses the provided bytes into {@link PayoutAddressSetEvent} event data. */
export function parsePayoutAddressSetEvent(
  data: ReadonlyUint8Array | Uint8Array,
): PayoutAddressSetEvent {
  if (containsBytes(data, PAYOUT_ADDRESS_SET_EVENT_DISCRIMINATOR, 0)) {
    return getPayoutAddressSetEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "PayoutAddressSetEvent" event discriminators.',
  );
}
