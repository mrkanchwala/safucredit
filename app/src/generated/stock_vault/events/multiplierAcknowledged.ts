/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const MULTIPLIER_ACKNOWLEDGED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([76, 31, 212, 210, 30, 10, 1, 159]);

export function getMultiplierAcknowledgedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MULTIPLIER_ACKNOWLEDGED_EVENT_DISCRIMINATOR,
  );
}

export type MultiplierAcknowledgedEvent = {
  market: Address;
  previous: bigint;
  effective: bigint;
};

export type MultiplierAcknowledgedEventArgs = {
  market: Address;
  previous: number | bigint;
  effective: number | bigint;
};

/** Gets the encoder for {@link MultiplierAcknowledgedEventArgs} event data. */
export function getMultiplierAcknowledgedEventEncoder(): FixedSizeEncoder<MultiplierAcknowledgedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["market", getAddressEncoder()],
      ["previous", getU128Encoder()],
      ["effective", getU128Encoder()],
    ]),
    [getConstantEncoder(MULTIPLIER_ACKNOWLEDGED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link MultiplierAcknowledgedEvent} event data. */
export function getMultiplierAcknowledgedEventDecoder(): FixedSizeDecoder<MultiplierAcknowledgedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["market", getAddressDecoder()],
      ["previous", getU128Decoder()],
      ["effective", getU128Decoder()],
    ]),
    [getConstantDecoder(MULTIPLIER_ACKNOWLEDGED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link MultiplierAcknowledgedEvent} event data. */
export function getMultiplierAcknowledgedEventCodec(): FixedSizeCodec<
  MultiplierAcknowledgedEventArgs,
  MultiplierAcknowledgedEvent
> {
  return combineCodec(
    getMultiplierAcknowledgedEventEncoder(),
    getMultiplierAcknowledgedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link MultiplierAcknowledgedEvent} event data. */
export function parseMultiplierAcknowledgedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): MultiplierAcknowledgedEvent {
  if (containsBytes(data, MULTIPLIER_ACKNOWLEDGED_EVENT_DISCRIMINATOR, 0)) {
    return getMultiplierAcknowledgedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "MultiplierAcknowledgedEvent" event discriminators.',
  );
}
