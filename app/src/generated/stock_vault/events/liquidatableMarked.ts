/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LIQUIDATABLE_MARKED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([136, 26, 157, 205, 66, 149, 57, 172]);

export function getLiquidatableMarkedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LIQUIDATABLE_MARKED_EVENT_DISCRIMINATOR,
  );
}

export type LiquidatableMarkedEvent = {
  position: Address;
  firstSeen: bigint;
  lastSeen: bigint;
};

export type LiquidatableMarkedEventArgs = {
  position: Address;
  firstSeen: number | bigint;
  lastSeen: number | bigint;
};

/** Gets the encoder for {@link LiquidatableMarkedEventArgs} event data. */
export function getLiquidatableMarkedEventEncoder(): FixedSizeEncoder<LiquidatableMarkedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["position", getAddressEncoder()],
      ["firstSeen", getI64Encoder()],
      ["lastSeen", getI64Encoder()],
    ]),
    [getConstantEncoder(LIQUIDATABLE_MARKED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LiquidatableMarkedEvent} event data. */
export function getLiquidatableMarkedEventDecoder(): FixedSizeDecoder<LiquidatableMarkedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["position", getAddressDecoder()],
      ["firstSeen", getI64Decoder()],
      ["lastSeen", getI64Decoder()],
    ]),
    [getConstantDecoder(LIQUIDATABLE_MARKED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LiquidatableMarkedEvent} event data. */
export function getLiquidatableMarkedEventCodec(): FixedSizeCodec<
  LiquidatableMarkedEventArgs,
  LiquidatableMarkedEvent
> {
  return combineCodec(
    getLiquidatableMarkedEventEncoder(),
    getLiquidatableMarkedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LiquidatableMarkedEvent} event data. */
export function parseLiquidatableMarkedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LiquidatableMarkedEvent {
  if (containsBytes(data, LIQUIDATABLE_MARKED_EVENT_DISCRIMINATOR, 0)) {
    return getLiquidatableMarkedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LiquidatableMarkedEvent" event discriminators.',
  );
}
