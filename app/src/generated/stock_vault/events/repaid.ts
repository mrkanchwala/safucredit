/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const REPAID_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  38, 248, 231, 7, 150, 164, 172, 23,
]);

export function getRepaidEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REPAID_EVENT_DISCRIMINATOR,
  );
}

export type RepaidEvent = { owner: Address; amount: bigint; shares: bigint };

export type RepaidEventArgs = {
  owner: Address;
  amount: number | bigint;
  shares: number | bigint;
};

/** Gets the encoder for {@link RepaidEventArgs} event data. */
export function getRepaidEventEncoder(): FixedSizeEncoder<RepaidEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["owner", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["shares", getU128Encoder()],
    ]),
    [getConstantEncoder(REPAID_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link RepaidEvent} event data. */
export function getRepaidEventDecoder(): FixedSizeDecoder<RepaidEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["owner", getAddressDecoder()],
      ["amount", getU64Decoder()],
      ["shares", getU128Decoder()],
    ]),
    [getConstantDecoder(REPAID_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link RepaidEvent} event data. */
export function getRepaidEventCodec(): FixedSizeCodec<
  RepaidEventArgs,
  RepaidEvent
> {
  return combineCodec(getRepaidEventEncoder(), getRepaidEventDecoder());
}

/** Parses the provided bytes into {@link RepaidEvent} event data. */
export function parseRepaidEvent(
  data: ReadonlyUint8Array | Uint8Array,
): RepaidEvent {
  if (containsBytes(data, REPAID_EVENT_DISCRIMINATOR, 0)) {
    return getRepaidEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "RepaidEvent" event discriminators.',
  );
}
