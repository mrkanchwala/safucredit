/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/program-client-core";
import { findBackerPda } from "../pdas";
import { BACKSTOP_PROGRAM_ADDRESS } from "../programs";

export const REQUEST_WITHDRAW_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([137, 95, 187, 96, 250, 138, 31, 182]);

export function getRequestWithdrawDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_WITHDRAW_DISCRIMINATOR,
  );
}

export type RequestWithdrawInstruction<
  TProgram extends string = typeof BACKSTOP_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountBacker extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountBacker extends string
        ? WritableAccount<TAccountBacker>
        : TAccountBacker,
      ...TRemainingAccounts,
    ]
  >;

export type RequestWithdrawInstructionData = {
  discriminator: ReadonlyUint8Array;
  shares: bigint;
};

export type RequestWithdrawInstructionDataArgs = { shares: number | bigint };

export function getRequestWithdrawInstructionDataEncoder(): FixedSizeEncoder<RequestWithdrawInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["shares", getU128Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REQUEST_WITHDRAW_DISCRIMINATOR }),
  );
}

export function getRequestWithdrawInstructionDataDecoder(): FixedSizeDecoder<RequestWithdrawInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["shares", getU128Decoder()],
  ]);
}

export function getRequestWithdrawInstructionDataCodec(): FixedSizeCodec<
  RequestWithdrawInstructionDataArgs,
  RequestWithdrawInstructionData
> {
  return combineCodec(
    getRequestWithdrawInstructionDataEncoder(),
    getRequestWithdrawInstructionDataDecoder(),
  );
}

export type RequestWithdrawAsyncInput<
  TAccountOwner extends InstructionSignerInput = InstructionSignerInput,
  TAccountBacker extends InstructionAccountInput = InstructionAccountInput,
> = {
  owner: TAccountOwner;
  backer?: TAccountBacker;
  shares: RequestWithdrawInstructionDataArgs["shares"];
};

export async function getRequestWithdrawInstructionAsync<
  TAccountOwner extends InstructionSignerInput,
  TAccountBacker extends InstructionAccountInput,
  TProgramAddress extends Address = typeof BACKSTOP_PROGRAM_ADDRESS,
>(
  input: RequestWithdrawAsyncInput<TAccountOwner, TAccountBacker>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RequestWithdrawInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountOwner,
      InstructionAccountInputAddress<TAccountOwner>
    >,
    ResolvedInstructionAccountMeta<
      TAccountBacker,
      InstructionAccountInputAddress<TAccountBacker>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BACKSTOP_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isSigner: true, isWritable: false },
    backer: { value: input.backer ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.backer.value) {
    accounts.backer.value = await findBackerPda(
      {
        owner: getAddressFromResolvedInstructionAccount(
          "owner",
          accounts.owner.value,
        ),
      },
      { programAddress },
    );
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("backer", accounts.backer),
    ],
    data: getRequestWithdrawInstructionDataEncoder().encode(
      args as RequestWithdrawInstructionDataArgs,
    ),
    programAddress,
  } as RequestWithdrawInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountOwner,
      InstructionAccountInputAddress<TAccountOwner>
    >,
    ResolvedInstructionAccountMeta<
      TAccountBacker,
      InstructionAccountInputAddress<TAccountBacker>
    >
  >);
}

export type RequestWithdrawInput<
  TAccountOwner extends InstructionSignerInput = InstructionSignerInput,
  TAccountBacker extends InstructionAccountInput = InstructionAccountInput,
> = {
  owner: TAccountOwner;
  backer: TAccountBacker;
  shares: RequestWithdrawInstructionDataArgs["shares"];
};

export function getRequestWithdrawInstruction<
  TAccountOwner extends InstructionSignerInput,
  TAccountBacker extends InstructionAccountInput,
  TProgramAddress extends Address = typeof BACKSTOP_PROGRAM_ADDRESS,
>(
  input: RequestWithdrawInput<TAccountOwner, TAccountBacker>,
  config?: { programAddress?: TProgramAddress },
): RequestWithdrawInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountOwner,
    InstructionAccountInputAddress<TAccountOwner>
  >,
  ResolvedInstructionAccountMeta<
    TAccountBacker,
    InstructionAccountInputAddress<TAccountBacker>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BACKSTOP_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isSigner: true, isWritable: false },
    backer: { value: input.backer ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("backer", accounts.backer),
    ],
    data: getRequestWithdrawInstructionDataEncoder().encode(
      args as RequestWithdrawInstructionDataArgs,
    ),
    programAddress,
  } as RequestWithdrawInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountOwner,
      InstructionAccountInputAddress<TAccountOwner>
    >,
    ResolvedInstructionAccountMeta<
      TAccountBacker,
      InstructionAccountInputAddress<TAccountBacker>
    >
  >);
}

export type ParsedRequestWithdrawInstruction<
  TProgram extends string = typeof BACKSTOP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    backer: TAccountMetas[1];
  };
  data: RequestWithdrawInstructionData;
};

export function parseRequestWithdrawInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestWithdrawInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), backer: getNextAccount() },
    data: getRequestWithdrawInstructionDataDecoder().decode(instruction.data),
  };
}
