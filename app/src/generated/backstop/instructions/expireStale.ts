/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { BACKSTOP_PROGRAM_ADDRESS } from "../programs";

export const EXPIRE_STALE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  78, 105, 16, 67, 30, 126, 71, 208,
]);

export function getExpireStaleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXPIRE_STALE_DISCRIMINATOR,
  );
}

export type ExpireStaleInstruction<
  TProgram extends string = typeof BACKSTOP_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountClaim extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountClaim extends string
        ? WritableAccount<TAccountClaim>
        : TAccountClaim,
      ...TRemainingAccounts,
    ]
  >;

export type ExpireStaleInstructionData = { discriminator: ReadonlyUint8Array };

export type ExpireStaleInstructionDataArgs = {};

export function getExpireStaleInstructionDataEncoder(): FixedSizeEncoder<ExpireStaleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: EXPIRE_STALE_DISCRIMINATOR }),
  );
}

export function getExpireStaleInstructionDataDecoder(): FixedSizeDecoder<ExpireStaleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExpireStaleInstructionDataCodec(): FixedSizeCodec<
  ExpireStaleInstructionDataArgs,
  ExpireStaleInstructionData
> {
  return combineCodec(
    getExpireStaleInstructionDataEncoder(),
    getExpireStaleInstructionDataDecoder(),
  );
}

export type ExpireStaleAsyncInput<
  TAccountConfig extends InstructionAccountInput = InstructionAccountInput,
  TAccountClaim extends InstructionAccountInput = InstructionAccountInput,
> = {
  config?: TAccountConfig;
  claim: TAccountClaim;
};

export async function getExpireStaleInstructionAsync<
  TAccountConfig extends InstructionAccountInput,
  TAccountClaim extends InstructionAccountInput,
  TProgramAddress extends Address = typeof BACKSTOP_PROGRAM_ADDRESS,
>(
  input: ExpireStaleAsyncInput<TAccountConfig, TAccountClaim>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ExpireStaleInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountConfig,
      InstructionAccountInputAddress<TAccountConfig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountClaim,
      InstructionAccountInputAddress<TAccountClaim>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BACKSTOP_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isSigner: false, isWritable: true },
    claim: { value: input.claim ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("claim", accounts.claim),
    ],
    data: getExpireStaleInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireStaleInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountConfig,
      InstructionAccountInputAddress<TAccountConfig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountClaim,
      InstructionAccountInputAddress<TAccountClaim>
    >
  >);
}

export type ExpireStaleInput<
  TAccountConfig extends InstructionAccountInput = InstructionAccountInput,
  TAccountClaim extends InstructionAccountInput = InstructionAccountInput,
> = {
  config: TAccountConfig;
  claim: TAccountClaim;
};

export function getExpireStaleInstruction<
  TAccountConfig extends InstructionAccountInput,
  TAccountClaim extends InstructionAccountInput,
  TProgramAddress extends Address = typeof BACKSTOP_PROGRAM_ADDRESS,
>(
  input: ExpireStaleInput<TAccountConfig, TAccountClaim>,
  config?: { programAddress?: TProgramAddress },
): ExpireStaleInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountConfig,
    InstructionAccountInputAddress<TAccountConfig>
  >,
  ResolvedInstructionAccountMeta<
    TAccountClaim,
    InstructionAccountInputAddress<TAccountClaim>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BACKSTOP_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isSigner: false, isWritable: true },
    claim: { value: input.claim ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("claim", accounts.claim),
    ],
    data: getExpireStaleInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireStaleInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountConfig,
      InstructionAccountInputAddress<TAccountConfig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountClaim,
      InstructionAccountInputAddress<TAccountClaim>
    >
  >);
}

export type ParsedExpireStaleInstruction<
  TProgram extends string = typeof BACKSTOP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    claim: TAccountMetas[1];
  };
  data: ExpireStaleInstructionData;
};

export function parseExpireStaleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExpireStaleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { config: getNextAccount(), claim: getNextAccount() },
    data: getExpireStaleInstructionDataDecoder().decode(instruction.data),
  };
}
