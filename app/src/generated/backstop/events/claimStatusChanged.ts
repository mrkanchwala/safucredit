/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getClaimStatusDecoder,
  getClaimStatusEncoder,
  type ClaimStatus,
  type ClaimStatusArgs,
} from "../types";

export const CLAIM_STATUS_CHANGED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([216, 31, 249, 76, 119, 54, 103, 65]);

export function getClaimStatusChangedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_STATUS_CHANGED_EVENT_DISCRIMINATOR,
  );
}

export type ClaimStatusChangedEvent = { claim: Address; status: ClaimStatus };

export type ClaimStatusChangedEventArgs = {
  claim: Address;
  status: ClaimStatusArgs;
};

/** Gets the encoder for {@link ClaimStatusChangedEventArgs} event data. */
export function getClaimStatusChangedEventEncoder(): FixedSizeEncoder<ClaimStatusChangedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["claim", getAddressEncoder()],
      ["status", getClaimStatusEncoder()],
    ]),
    [getConstantEncoder(CLAIM_STATUS_CHANGED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link ClaimStatusChangedEvent} event data. */
export function getClaimStatusChangedEventDecoder(): FixedSizeDecoder<ClaimStatusChangedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["claim", getAddressDecoder()],
      ["status", getClaimStatusDecoder()],
    ]),
    [getConstantDecoder(CLAIM_STATUS_CHANGED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link ClaimStatusChangedEvent} event data. */
export function getClaimStatusChangedEventCodec(): FixedSizeCodec<
  ClaimStatusChangedEventArgs,
  ClaimStatusChangedEvent
> {
  return combineCodec(
    getClaimStatusChangedEventEncoder(),
    getClaimStatusChangedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link ClaimStatusChangedEvent} event data. */
export function parseClaimStatusChangedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): ClaimStatusChangedEvent {
  if (containsBytes(data, CLAIM_STATUS_CHANGED_EVENT_DISCRIMINATOR, 0)) {
    return getClaimStatusChangedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "ClaimStatusChangedEvent" event discriminators.',
  );
}
