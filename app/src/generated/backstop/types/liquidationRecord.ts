/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/**
 * Immutable snapshot of one liquidation.
 *
 * The verdict engine decides wrongfulness off-chain, so it must be able to re-derive the decision
 * from what the contract actually used — not from whatever the feed says later. Every input that
 * fed the seizure is recorded here, including `issuer_halt` at the time, because a liquidation
 * taken while the issuer had intervened is excluded from cover (spec 14a).
 */
export type LiquidationRecord = {
  version: number;
  bump: number;
  market: Address;
  seq: bigint;
  borrower: Address;
  liquidator: Address;
  /** Raw collateral handed to the liquidator. */
  seizedRaw: bigint;
  /** USDC the liquidator repaid on the borrower's behalf. */
  debtRepaid: bigint;
  /** Effective multiplier used for the valuation, in `MULT_SCALE` fixed point. */
  multiplierFp: bigint;
  /** TWAP the seizure was priced at, 8 decimals. */
  priceFp: bigint;
  collateralDecimals: number;
  bonusBps: number;
  /** Position LTV at the moment of seizure. */
  ltvBps: number;
  /** Coverage in force for this borrower when the loan was opened (U3). */
  coverageBps: number;
  /** The loan's debt-weighted borrow time at the moment of liquidation (for the 60-day gate). */
  borrowAgeTs: bigint;
  /** Payback address at the moment of liquidation. Frozen here; later changes cannot redirect it. */
  payout: Address;
  /** Debt written off because the collateral ran out, 0 in the normal case. */
  badDebt: bigint;
  ts: bigint;
  slot: bigint;
  issuerHalt: boolean;
  reserved: ReadonlyUint8Array;
};

export type LiquidationRecordArgs = {
  version: number;
  bump: number;
  market: Address;
  seq: number | bigint;
  borrower: Address;
  liquidator: Address;
  /** Raw collateral handed to the liquidator. */
  seizedRaw: number | bigint;
  /** USDC the liquidator repaid on the borrower's behalf. */
  debtRepaid: number | bigint;
  /** Effective multiplier used for the valuation, in `MULT_SCALE` fixed point. */
  multiplierFp: number | bigint;
  /** TWAP the seizure was priced at, 8 decimals. */
  priceFp: number | bigint;
  collateralDecimals: number;
  bonusBps: number;
  /** Position LTV at the moment of seizure. */
  ltvBps: number;
  /** Coverage in force for this borrower when the loan was opened (U3). */
  coverageBps: number;
  /** The loan's debt-weighted borrow time at the moment of liquidation (for the 60-day gate). */
  borrowAgeTs: number | bigint;
  /** Payback address at the moment of liquidation. Frozen here; later changes cannot redirect it. */
  payout: Address;
  /** Debt written off because the collateral ran out, 0 in the normal case. */
  badDebt: number | bigint;
  ts: number | bigint;
  slot: number | bigint;
  issuerHalt: boolean;
  reserved: ReadonlyUint8Array;
};

export function getLiquidationRecordEncoder(): FixedSizeEncoder<LiquidationRecordArgs> {
  return getStructEncoder([
    ["version", getU8Encoder()],
    ["bump", getU8Encoder()],
    ["market", getAddressEncoder()],
    ["seq", getU64Encoder()],
    ["borrower", getAddressEncoder()],
    ["liquidator", getAddressEncoder()],
    ["seizedRaw", getU64Encoder()],
    ["debtRepaid", getU64Encoder()],
    ["multiplierFp", getU128Encoder()],
    ["priceFp", getU64Encoder()],
    ["collateralDecimals", getU8Encoder()],
    ["bonusBps", getU32Encoder()],
    ["ltvBps", getU32Encoder()],
    ["coverageBps", getU32Encoder()],
    ["borrowAgeTs", getI64Encoder()],
    ["payout", getAddressEncoder()],
    ["badDebt", getU64Encoder()],
    ["ts", getI64Encoder()],
    ["slot", getU64Encoder()],
    ["issuerHalt", getBooleanEncoder()],
    ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
  ]);
}

export function getLiquidationRecordDecoder(): FixedSizeDecoder<LiquidationRecord> {
  return getStructDecoder([
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["market", getAddressDecoder()],
    ["seq", getU64Decoder()],
    ["borrower", getAddressDecoder()],
    ["liquidator", getAddressDecoder()],
    ["seizedRaw", getU64Decoder()],
    ["debtRepaid", getU64Decoder()],
    ["multiplierFp", getU128Decoder()],
    ["priceFp", getU64Decoder()],
    ["collateralDecimals", getU8Decoder()],
    ["bonusBps", getU32Decoder()],
    ["ltvBps", getU32Decoder()],
    ["coverageBps", getU32Decoder()],
    ["borrowAgeTs", getI64Decoder()],
    ["payout", getAddressDecoder()],
    ["badDebt", getU64Decoder()],
    ["ts", getI64Decoder()],
    ["slot", getU64Decoder()],
    ["issuerHalt", getBooleanDecoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getLiquidationRecordCodec(): FixedSizeCodec<
  LiquidationRecordArgs,
  LiquidationRecord
> {
  return combineCodec(
    getLiquidationRecordEncoder(),
    getLiquidationRecordDecoder(),
  );
}
