/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export type VaultConfig = {
  version: number;
  admin: Address;
  /** Pushes prices through the mock adapter on devnet. Separate key from the verdict oracle. */
  feedAuthority: Address;
  paused: boolean;
  bump: number;
  /**
   * The backstop pool's liquidator key (its config PDA). It may liquidate at once; anyone else only after
   * the grace period. Default = no pool registered, so the grace rule applies to every liquidator.
   */
  poolLiquidator: Address;
  /** How long a position must have been liquidatable before an outside liquidator may act. 900 s. */
  fallbackGraceSecs: bigint;
  /**
   * Which cluster this deployment is on (0 localnet, 1 devnet, 2 mainnet), set once at initialize with no
   * setter. Selects the time floors (eng review A5): mainnet may never shorten the grace below 5 min.
   */
  clusterTag: number;
  /** Taken from the old 64 reserved bytes (32 + 8 + 1), so the account size is unchanged (U4). */
  reserved: ReadonlyUint8Array;
};

export type VaultConfigArgs = {
  version: number;
  admin: Address;
  /** Pushes prices through the mock adapter on devnet. Separate key from the verdict oracle. */
  feedAuthority: Address;
  paused: boolean;
  bump: number;
  /**
   * The backstop pool's liquidator key (its config PDA). It may liquidate at once; anyone else only after
   * the grace period. Default = no pool registered, so the grace rule applies to every liquidator.
   */
  poolLiquidator: Address;
  /** How long a position must have been liquidatable before an outside liquidator may act. 900 s. */
  fallbackGraceSecs: number | bigint;
  /**
   * Which cluster this deployment is on (0 localnet, 1 devnet, 2 mainnet), set once at initialize with no
   * setter. Selects the time floors (eng review A5): mainnet may never shorten the grace below 5 min.
   */
  clusterTag: number;
  /** Taken from the old 64 reserved bytes (32 + 8 + 1), so the account size is unchanged (U4). */
  reserved: ReadonlyUint8Array;
};

export function getVaultConfigEncoder(): FixedSizeEncoder<VaultConfigArgs> {
  return getStructEncoder([
    ["version", getU8Encoder()],
    ["admin", getAddressEncoder()],
    ["feedAuthority", getAddressEncoder()],
    ["paused", getBooleanEncoder()],
    ["bump", getU8Encoder()],
    ["poolLiquidator", getAddressEncoder()],
    ["fallbackGraceSecs", getI64Encoder()],
    ["clusterTag", getU8Encoder()],
    ["reserved", fixEncoderSize(getBytesEncoder(), 23)],
  ]);
}

export function getVaultConfigDecoder(): FixedSizeDecoder<VaultConfig> {
  return getStructDecoder([
    ["version", getU8Decoder()],
    ["admin", getAddressDecoder()],
    ["feedAuthority", getAddressDecoder()],
    ["paused", getBooleanDecoder()],
    ["bump", getU8Decoder()],
    ["poolLiquidator", getAddressDecoder()],
    ["fallbackGraceSecs", getI64Decoder()],
    ["clusterTag", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 23)],
  ]);
}

export function getVaultConfigCodec(): FixedSizeCodec<
  VaultConfigArgs,
  VaultConfig
> {
  return combineCodec(getVaultConfigEncoder(), getVaultConfigDecoder());
}
