/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const BACKSTOP_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [51, 117, 28, 87, 198, 48, 159, 164],
);

export function getBackstopConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BACKSTOP_CONFIG_DISCRIMINATOR,
  );
}

export type BackstopConfig = {
  discriminator: ReadonlyUint8Array;
  version: number;
  admin: Address;
  /** Throwaway per-chain Ed25519 key. Never the production KMS key. */
  verdictOracle: Address;
  /**
   * Phase 4: the second signer of the 2-of-2 override. Always distinct from `admin` and
   * `verdict_oracle` (LOCKED verdict spec: "admin ≠ oracle ≠ co-signer, at init and on every
   * setter"), enforced at `initialize_backstop` and every one of the three setters.
   */
  coSigner: Address;
  /** Bound into every signed verdict so a devnet signature can never verify on mainnet. */
  clusterTag: number;
  bump: number;
  usdcMint: Address;
  /**
   * Backer capital actually paid in, tracked internally. Never read from the token account, so a
   * raw transfer into the vault cannot move the share price (same rule as the lending market).
   */
  cash: bigint;
  totalShares: bigint;
  /**
   * Ceiling on a single claim's admission, as a share of cash checked at admission time. A loss
   * above it never shrinks — it queues until cash grows or utilisation falls enough to admit it.
   */
  perClaimCapBps: number;
  /**
   * Sum of `loss − streamed` across every `Active` claim. Backers may withdraw `cash −
   * reserved_total`; a queued or held claim reserves nothing until it is actually admitted.
   */
  reservedTotal: bigint;
  /** Day bucket (`now / DAY_SECS`) the admission counter below is tracking. */
  admissionDay: bigint;
  /** Cumulative loss admitted so far within `admission_day`. */
  admittedToday: bigint;
  /** Delay between requesting an exit and taking the money. */
  withdrawDelaySecs: bigint;
  /**
   * Phase 3 (pool-as-liquidator). Sum of every market's `Inventory.cost_total`: while positive,
   * `deposit` and `finalize_withdraw` refuse (D4) -- backers cannot buy in cheap right after a
   * liquidation or exit before a crash-holding is resolved.
   */
  inventoryCostTotal: bigint;
  /** Max share of `cash` a single `pool_liquidate` call may spend. */
  perLiqCapBps: number;
  /** Max share of `cash` `pool_liquidate` may spend across one day, combined. */
  dailyLiqCapBps: number;
  /**
   * Day bucket (`now / DAY_SECS`) `liq_spent_today` is tracking. Separate from the claim
   * admission day counter -- liquidation draws and claim admissions are different budgets.
   */
  liqDay: bigint;
  liqSpentToday: bigint;
  /**
   * Resale discount off the healthy price, and how long a fresh acquisition may not be resold
   * below its own cost (locked: never below cost for the first 4 days).
   */
  resaleDiscountBps: number;
  resaleFloorSecs: bigint;
  /**
   * Crank fee, as a share of the liquidation bonus the pool captured (D6). Paid to whoever calls
   * `pool_liquidate`, from cash, after the CPI succeeds.
   */
  feeShareBps: number;
  /**
   * Below this, `pool_liquidate` does not fire -- a real liquidation is always well above it; this
   * only blocks dust-repay fee farming (E6).
   */
  minPoolRepay: bigint;
  /** Phase 5 (A5): admin-settable claim timings, bounded per `cluster_tag` by `set_claim_timing`. */
  gateSecs: bigint;
  cooldownSecs: bigint;
  streamSecs: bigint;
  inactivitySecs: bigint;
  minAfterWaitSecs: bigint;
  /** Room for later fields without a migration (U4). */
  reserved: ReadonlyUint8Array;
};

export type BackstopConfigArgs = {
  version: number;
  admin: Address;
  /** Throwaway per-chain Ed25519 key. Never the production KMS key. */
  verdictOracle: Address;
  /**
   * Phase 4: the second signer of the 2-of-2 override. Always distinct from `admin` and
   * `verdict_oracle` (LOCKED verdict spec: "admin ≠ oracle ≠ co-signer, at init and on every
   * setter"), enforced at `initialize_backstop` and every one of the three setters.
   */
  coSigner: Address;
  /** Bound into every signed verdict so a devnet signature can never verify on mainnet. */
  clusterTag: number;
  bump: number;
  usdcMint: Address;
  /**
   * Backer capital actually paid in, tracked internally. Never read from the token account, so a
   * raw transfer into the vault cannot move the share price (same rule as the lending market).
   */
  cash: number | bigint;
  totalShares: number | bigint;
  /**
   * Ceiling on a single claim's admission, as a share of cash checked at admission time. A loss
   * above it never shrinks — it queues until cash grows or utilisation falls enough to admit it.
   */
  perClaimCapBps: number;
  /**
   * Sum of `loss − streamed` across every `Active` claim. Backers may withdraw `cash −
   * reserved_total`; a queued or held claim reserves nothing until it is actually admitted.
   */
  reservedTotal: number | bigint;
  /** Day bucket (`now / DAY_SECS`) the admission counter below is tracking. */
  admissionDay: number | bigint;
  /** Cumulative loss admitted so far within `admission_day`. */
  admittedToday: number | bigint;
  /** Delay between requesting an exit and taking the money. */
  withdrawDelaySecs: number | bigint;
  /**
   * Phase 3 (pool-as-liquidator). Sum of every market's `Inventory.cost_total`: while positive,
   * `deposit` and `finalize_withdraw` refuse (D4) -- backers cannot buy in cheap right after a
   * liquidation or exit before a crash-holding is resolved.
   */
  inventoryCostTotal: number | bigint;
  /** Max share of `cash` a single `pool_liquidate` call may spend. */
  perLiqCapBps: number;
  /** Max share of `cash` `pool_liquidate` may spend across one day, combined. */
  dailyLiqCapBps: number;
  /**
   * Day bucket (`now / DAY_SECS`) `liq_spent_today` is tracking. Separate from the claim
   * admission day counter -- liquidation draws and claim admissions are different budgets.
   */
  liqDay: number | bigint;
  liqSpentToday: number | bigint;
  /**
   * Resale discount off the healthy price, and how long a fresh acquisition may not be resold
   * below its own cost (locked: never below cost for the first 4 days).
   */
  resaleDiscountBps: number;
  resaleFloorSecs: number | bigint;
  /**
   * Crank fee, as a share of the liquidation bonus the pool captured (D6). Paid to whoever calls
   * `pool_liquidate`, from cash, after the CPI succeeds.
   */
  feeShareBps: number;
  /**
   * Below this, `pool_liquidate` does not fire -- a real liquidation is always well above it; this
   * only blocks dust-repay fee farming (E6).
   */
  minPoolRepay: number | bigint;
  /** Phase 5 (A5): admin-settable claim timings, bounded per `cluster_tag` by `set_claim_timing`. */
  gateSecs: number | bigint;
  cooldownSecs: number | bigint;
  streamSecs: number | bigint;
  inactivitySecs: number | bigint;
  minAfterWaitSecs: number | bigint;
  /** Room for later fields without a migration (U4). */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link BackstopConfigArgs} account data. */
export function getBackstopConfigEncoder(): FixedSizeEncoder<BackstopConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["version", getU8Encoder()],
      ["admin", getAddressEncoder()],
      ["verdictOracle", getAddressEncoder()],
      ["coSigner", getAddressEncoder()],
      ["clusterTag", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["usdcMint", getAddressEncoder()],
      ["cash", getU64Encoder()],
      ["totalShares", getU128Encoder()],
      ["perClaimCapBps", getU32Encoder()],
      ["reservedTotal", getU64Encoder()],
      ["admissionDay", getI64Encoder()],
      ["admittedToday", getU64Encoder()],
      ["withdrawDelaySecs", getI64Encoder()],
      ["inventoryCostTotal", getU64Encoder()],
      ["perLiqCapBps", getU32Encoder()],
      ["dailyLiqCapBps", getU32Encoder()],
      ["liqDay", getI64Encoder()],
      ["liqSpentToday", getU64Encoder()],
      ["resaleDiscountBps", getU32Encoder()],
      ["resaleFloorSecs", getI64Encoder()],
      ["feeShareBps", getU32Encoder()],
      ["minPoolRepay", getU64Encoder()],
      ["gateSecs", getI64Encoder()],
      ["cooldownSecs", getI64Encoder()],
      ["streamSecs", getI64Encoder()],
      ["inactivitySecs", getI64Encoder()],
      ["minAfterWaitSecs", getI64Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 16)],
    ]),
    (value) => ({ ...value, discriminator: BACKSTOP_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link BackstopConfig} account data. */
export function getBackstopConfigDecoder(): FixedSizeDecoder<BackstopConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["version", getU8Decoder()],
    ["admin", getAddressDecoder()],
    ["verdictOracle", getAddressDecoder()],
    ["coSigner", getAddressDecoder()],
    ["clusterTag", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["usdcMint", getAddressDecoder()],
    ["cash", getU64Decoder()],
    ["totalShares", getU128Decoder()],
    ["perClaimCapBps", getU32Decoder()],
    ["reservedTotal", getU64Decoder()],
    ["admissionDay", getI64Decoder()],
    ["admittedToday", getU64Decoder()],
    ["withdrawDelaySecs", getI64Decoder()],
    ["inventoryCostTotal", getU64Decoder()],
    ["perLiqCapBps", getU32Decoder()],
    ["dailyLiqCapBps", getU32Decoder()],
    ["liqDay", getI64Decoder()],
    ["liqSpentToday", getU64Decoder()],
    ["resaleDiscountBps", getU32Decoder()],
    ["resaleFloorSecs", getI64Decoder()],
    ["feeShareBps", getU32Decoder()],
    ["minPoolRepay", getU64Decoder()],
    ["gateSecs", getI64Decoder()],
    ["cooldownSecs", getI64Decoder()],
    ["streamSecs", getI64Decoder()],
    ["inactivitySecs", getI64Decoder()],
    ["minAfterWaitSecs", getI64Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 16)],
  ]);
}

/** Gets the codec for {@link BackstopConfig} account data. */
export function getBackstopConfigCodec(): FixedSizeCodec<
  BackstopConfigArgs,
  BackstopConfig
> {
  return combineCodec(getBackstopConfigEncoder(), getBackstopConfigDecoder());
}

export function decodeBackstopConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<BackstopConfig, TAddress>;
export function decodeBackstopConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<BackstopConfig, TAddress>;
export function decodeBackstopConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<BackstopConfig, TAddress> | MaybeAccount<BackstopConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBackstopConfigDecoder(),
  );
}

export async function fetchBackstopConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<BackstopConfig, TAddress>> {
  const maybeAccount = await fetchMaybeBackstopConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBackstopConfig<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<BackstopConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBackstopConfig(maybeAccount);
}

export async function fetchAllBackstopConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<BackstopConfig>[]> {
  const maybeAccounts = await fetchAllMaybeBackstopConfig(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBackstopConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<BackstopConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeBackstopConfig(maybeAccount),
  );
}

export function getBackstopConfigSize(): number {
  return 311;
}
