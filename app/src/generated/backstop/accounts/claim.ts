/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getClaimStatusDecoder,
  getClaimStatusEncoder,
  type ClaimStatus,
  type ClaimStatusArgs,
} from "../types";

export const CLAIM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  155, 70, 22, 176, 123, 215, 246, 102,
]);

export function getClaimDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_DISCRIMINATOR);
}

export type Claim = {
  discriminator: ReadonlyUint8Array;
  version: number;
  bump: number;
  market: Address;
  borrower: Address;
  liquidationRecord: Address;
  /** Frozen at submission from the liquidation record; the eventual payout destination. */
  payout: Address;
  status: ClaimStatus;
  denyReason: number;
  /** `safu_core::wrongful_loss`, computed once at submission. Zero on a denied claim. */
  loss: bigint;
  /** Cumulative amount actually paid out via `claim_stream`. */
  streamed: bigint;
  submittedAt: bigint;
  /**
   * When this liquidation happened (`LiquidationRecord.ts`), copied here so later instructions
   * never need to re-fetch the vault's record.
   */
  liquidatedAt: bigint;
  /** `PendingTime` only: when the 60-day gate clears and `unlock_claim` may be called. */
  releasableAt: bigint;
  /** `Active` only: when admission happened. */
  admittedAt: bigint;
  cooldownEnd: bigint;
  streamEnd: bigint;
  /**
   * `cash` at the moment of admission — the outflow cap's floor, so a claim admitted against a
   * large pool cannot be strangled if the pool later shrinks.
   */
  snapshotCash: bigint;
  lastPullDay: bigint;
  pulledToday: bigint;
  /** Updated at admission and at every successful pull. `expire_stale` reads it. */
  lastActivityTs: bigint;
  evidenceHash: ReadonlyUint8Array;
  /**
   * Phase 4 (suspend/unsuspend). While true, every permissionless progression instruction
   * (`unlock_claim`, `try_release_queued`, `expire_queued`, `expire_stale`, `claim_stream`)
   * refuses. `suspended_since` is the wall-clock moment suspension began (0 when not
   * suspended); on `unsuspend_claim` its duration is folded into `suspended_secs`.
   */
  suspended: boolean;
  suspendedSince: bigint;
  /**
   * Cumulative seconds this claim has ever spent suspended. Subtracted from elapsed time in
   * `expire_queued`/`expire_stale` so a suspension genuinely freezes those clocks rather than
   * merely delaying when they are checked (LOCKED verdict spec: "expiry clocks stop while
   * suspended and reset on unsuspend").
   */
  suspendedSecs: bigint;
  /**
   * Reference point `claim_stream` measures vesting elapsed-time from. Defaults to
   * `cooldown_end` (identical to reading `cooldown_end` directly) for every ordinary
   * admission. A 2-of-2 override that carries a prior `streamed` amount forward shifts this
   * backward by that amount's time-equivalent, so vesting resumes from where it left off
   * instead of re-streaming the already-paid portion a second time.
   */
  vestOrigin: bigint;
  /**
   * Phase 5: `BackstopConfig.inactivity_secs` copied at admission, so a later change never shortens or
   * lengthens an already-admitted claim's expiry. The stream length is likewise frozen as
   * `stream_end − cooldown_end`.
   */
  inactivitySecs: bigint;
  reserved: ReadonlyUint8Array;
};

export type ClaimArgs = {
  version: number;
  bump: number;
  market: Address;
  borrower: Address;
  liquidationRecord: Address;
  /** Frozen at submission from the liquidation record; the eventual payout destination. */
  payout: Address;
  status: ClaimStatusArgs;
  denyReason: number;
  /** `safu_core::wrongful_loss`, computed once at submission. Zero on a denied claim. */
  loss: number | bigint;
  /** Cumulative amount actually paid out via `claim_stream`. */
  streamed: number | bigint;
  submittedAt: number | bigint;
  /**
   * When this liquidation happened (`LiquidationRecord.ts`), copied here so later instructions
   * never need to re-fetch the vault's record.
   */
  liquidatedAt: number | bigint;
  /** `PendingTime` only: when the 60-day gate clears and `unlock_claim` may be called. */
  releasableAt: number | bigint;
  /** `Active` only: when admission happened. */
  admittedAt: number | bigint;
  cooldownEnd: number | bigint;
  streamEnd: number | bigint;
  /**
   * `cash` at the moment of admission — the outflow cap's floor, so a claim admitted against a
   * large pool cannot be strangled if the pool later shrinks.
   */
  snapshotCash: number | bigint;
  lastPullDay: number | bigint;
  pulledToday: number | bigint;
  /** Updated at admission and at every successful pull. `expire_stale` reads it. */
  lastActivityTs: number | bigint;
  evidenceHash: ReadonlyUint8Array;
  /**
   * Phase 4 (suspend/unsuspend). While true, every permissionless progression instruction
   * (`unlock_claim`, `try_release_queued`, `expire_queued`, `expire_stale`, `claim_stream`)
   * refuses. `suspended_since` is the wall-clock moment suspension began (0 when not
   * suspended); on `unsuspend_claim` its duration is folded into `suspended_secs`.
   */
  suspended: boolean;
  suspendedSince: number | bigint;
  /**
   * Cumulative seconds this claim has ever spent suspended. Subtracted from elapsed time in
   * `expire_queued`/`expire_stale` so a suspension genuinely freezes those clocks rather than
   * merely delaying when they are checked (LOCKED verdict spec: "expiry clocks stop while
   * suspended and reset on unsuspend").
   */
  suspendedSecs: number | bigint;
  /**
   * Reference point `claim_stream` measures vesting elapsed-time from. Defaults to
   * `cooldown_end` (identical to reading `cooldown_end` directly) for every ordinary
   * admission. A 2-of-2 override that carries a prior `streamed` amount forward shifts this
   * backward by that amount's time-equivalent, so vesting resumes from where it left off
   * instead of re-streaming the already-paid portion a second time.
   */
  vestOrigin: number | bigint;
  /**
   * Phase 5: `BackstopConfig.inactivity_secs` copied at admission, so a later change never shortens or
   * lengthens an already-admitted claim's expiry. The stream length is likewise frozen as
   * `stream_end − cooldown_end`.
   */
  inactivitySecs: number | bigint;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link ClaimArgs} account data. */
export function getClaimEncoder(): FixedSizeEncoder<ClaimArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["market", getAddressEncoder()],
      ["borrower", getAddressEncoder()],
      ["liquidationRecord", getAddressEncoder()],
      ["payout", getAddressEncoder()],
      ["status", getClaimStatusEncoder()],
      ["denyReason", getU16Encoder()],
      ["loss", getU64Encoder()],
      ["streamed", getU64Encoder()],
      ["submittedAt", getI64Encoder()],
      ["liquidatedAt", getI64Encoder()],
      ["releasableAt", getI64Encoder()],
      ["admittedAt", getI64Encoder()],
      ["cooldownEnd", getI64Encoder()],
      ["streamEnd", getI64Encoder()],
      ["snapshotCash", getU64Encoder()],
      ["lastPullDay", getI64Encoder()],
      ["pulledToday", getU64Encoder()],
      ["lastActivityTs", getI64Encoder()],
      ["evidenceHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["suspended", getBooleanEncoder()],
      ["suspendedSince", getI64Encoder()],
      ["suspendedSecs", getI64Encoder()],
      ["vestOrigin", getI64Encoder()],
      ["inactivitySecs", getI64Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 8)],
    ]),
    (value) => ({ ...value, discriminator: CLAIM_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Claim} account data. */
export function getClaimDecoder(): FixedSizeDecoder<Claim> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["market", getAddressDecoder()],
    ["borrower", getAddressDecoder()],
    ["liquidationRecord", getAddressDecoder()],
    ["payout", getAddressDecoder()],
    ["status", getClaimStatusDecoder()],
    ["denyReason", getU16Decoder()],
    ["loss", getU64Decoder()],
    ["streamed", getU64Decoder()],
    ["submittedAt", getI64Decoder()],
    ["liquidatedAt", getI64Decoder()],
    ["releasableAt", getI64Decoder()],
    ["admittedAt", getI64Decoder()],
    ["cooldownEnd", getI64Decoder()],
    ["streamEnd", getI64Decoder()],
    ["snapshotCash", getU64Decoder()],
    ["lastPullDay", getI64Decoder()],
    ["pulledToday", getU64Decoder()],
    ["lastActivityTs", getI64Decoder()],
    ["evidenceHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["suspended", getBooleanDecoder()],
    ["suspendedSince", getI64Decoder()],
    ["suspendedSecs", getI64Decoder()],
    ["vestOrigin", getI64Decoder()],
    ["inactivitySecs", getI64Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

/** Gets the codec for {@link Claim} account data. */
export function getClaimCodec(): FixedSizeCodec<ClaimArgs, Claim> {
  return combineCodec(getClaimEncoder(), getClaimDecoder());
}

export function decodeClaim<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Claim, TAddress>;
export function decodeClaim<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Claim, TAddress>;
export function decodeClaim<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Claim, TAddress> | MaybeAccount<Claim, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getClaimDecoder(),
  );
}

export async function fetchClaim<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Claim, TAddress>> {
  const maybeAccount = await fetchMaybeClaim(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeClaim<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Claim, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeClaim(maybeAccount);
}

export async function fetchAllClaim(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Claim>[]> {
  const maybeAccounts = await fetchAllMaybeClaim(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeClaim(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Claim>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeClaim(maybeAccount));
}

export function getClaimSize(): number {
  return 310;
}
