/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const BORROWER_CLAIMS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [185, 75, 67, 68, 178, 239, 66, 251],
);

export function getBorrowerClaimsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BORROWER_CLAIMS_DISCRIMINATOR,
  );
}

export type BorrowerClaims = {
  discriminator: ReadonlyUint8Array;
  version: number;
  bump: number;
  market: Address;
  borrower: Address;
  /** `Pubkey::default()` when free. Cleared to the new claim once the occupant is terminal. */
  open: Address;
  /**
   * Phase 4: both set together by `cancel_claim` when a cancelled claim had already started
   * streaming — `penalty_since` at the cancellation, `penalty_until` 365 days later.
   * `submit_facts` denies (`deny_reason::PENALTY_ACTIVE`, does not revert) any liquidation whose
   * debt-weighted `borrow_age_ts` falls inside `[penalty_since, penalty_until)` — "no coverage
   * on new loans for 365 days" (LOCKED verdict spec), scoped to this exact (market, borrower)
   * pair. The lower bound matters: without it, a loan opened long before the penalty would
   * still satisfy `borrow_age_ts < penalty_until` forever, since that bound sits in the future
   * by construction — an old, already-covered loan must never be caught by a later penalty.
   */
  penaltySince: bigint;
  penaltyUntil: bigint;
  reserved: ReadonlyUint8Array;
};

export type BorrowerClaimsArgs = {
  version: number;
  bump: number;
  market: Address;
  borrower: Address;
  /** `Pubkey::default()` when free. Cleared to the new claim once the occupant is terminal. */
  open: Address;
  /**
   * Phase 4: both set together by `cancel_claim` when a cancelled claim had already started
   * streaming — `penalty_since` at the cancellation, `penalty_until` 365 days later.
   * `submit_facts` denies (`deny_reason::PENALTY_ACTIVE`, does not revert) any liquidation whose
   * debt-weighted `borrow_age_ts` falls inside `[penalty_since, penalty_until)` — "no coverage
   * on new loans for 365 days" (LOCKED verdict spec), scoped to this exact (market, borrower)
   * pair. The lower bound matters: without it, a loan opened long before the penalty would
   * still satisfy `borrow_age_ts < penalty_until` forever, since that bound sits in the future
   * by construction — an old, already-covered loan must never be caught by a later penalty.
   */
  penaltySince: number | bigint;
  penaltyUntil: number | bigint;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link BorrowerClaimsArgs} account data. */
export function getBorrowerClaimsEncoder(): FixedSizeEncoder<BorrowerClaimsArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["market", getAddressEncoder()],
      ["borrower", getAddressEncoder()],
      ["open", getAddressEncoder()],
      ["penaltySince", getI64Encoder()],
      ["penaltyUntil", getI64Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: BORROWER_CLAIMS_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link BorrowerClaims} account data. */
export function getBorrowerClaimsDecoder(): FixedSizeDecoder<BorrowerClaims> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["market", getAddressDecoder()],
    ["borrower", getAddressDecoder()],
    ["open", getAddressDecoder()],
    ["penaltySince", getI64Decoder()],
    ["penaltyUntil", getI64Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link BorrowerClaims} account data. */
export function getBorrowerClaimsCodec(): FixedSizeCodec<
  BorrowerClaimsArgs,
  BorrowerClaims
> {
  return combineCodec(getBorrowerClaimsEncoder(), getBorrowerClaimsDecoder());
}

export function decodeBorrowerClaims<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<BorrowerClaims, TAddress>;
export function decodeBorrowerClaims<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<BorrowerClaims, TAddress>;
export function decodeBorrowerClaims<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<BorrowerClaims, TAddress> | MaybeAccount<BorrowerClaims, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBorrowerClaimsDecoder(),
  );
}

export async function fetchBorrowerClaims<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<BorrowerClaims, TAddress>> {
  const maybeAccount = await fetchMaybeBorrowerClaims(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBorrowerClaims<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<BorrowerClaims, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBorrowerClaims(maybeAccount);
}

export async function fetchAllBorrowerClaims(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<BorrowerClaims>[]> {
  const maybeAccounts = await fetchAllMaybeBorrowerClaims(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBorrowerClaims(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<BorrowerClaims>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeBorrowerClaims(maybeAccount),
  );
}

export function getBorrowerClaimsSize(): number {
  return 154;
}
