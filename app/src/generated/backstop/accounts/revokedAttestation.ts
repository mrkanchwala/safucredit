/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const REVOKED_ATTESTATION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([40, 17, 219, 149, 165, 22, 221, 119]);

export function getRevokedAttestationDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKED_ATTESTATION_DISCRIMINATOR,
  );
}

export type RevokedAttestation = {
  discriminator: ReadonlyUint8Array;
  version: number;
  bump: number;
  liquidationRecord: Address;
  evidenceHash: ReadonlyUint8Array;
  revokedAt: bigint;
  reserved: ReadonlyUint8Array;
};

export type RevokedAttestationArgs = {
  version: number;
  bump: number;
  liquidationRecord: Address;
  evidenceHash: ReadonlyUint8Array;
  revokedAt: number | bigint;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link RevokedAttestationArgs} account data. */
export function getRevokedAttestationEncoder(): FixedSizeEncoder<RevokedAttestationArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["liquidationRecord", getAddressEncoder()],
      ["evidenceHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["revokedAt", getI64Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 16)],
    ]),
    (value) => ({ ...value, discriminator: REVOKED_ATTESTATION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link RevokedAttestation} account data. */
export function getRevokedAttestationDecoder(): FixedSizeDecoder<RevokedAttestation> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["liquidationRecord", getAddressDecoder()],
    ["evidenceHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["revokedAt", getI64Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 16)],
  ]);
}

/** Gets the codec for {@link RevokedAttestation} account data. */
export function getRevokedAttestationCodec(): FixedSizeCodec<
  RevokedAttestationArgs,
  RevokedAttestation
> {
  return combineCodec(
    getRevokedAttestationEncoder(),
    getRevokedAttestationDecoder(),
  );
}

export function decodeRevokedAttestation<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<RevokedAttestation, TAddress>;
export function decodeRevokedAttestation<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<RevokedAttestation, TAddress>;
export function decodeRevokedAttestation<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<RevokedAttestation, TAddress>
  | MaybeAccount<RevokedAttestation, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRevokedAttestationDecoder(),
  );
}

export async function fetchRevokedAttestation<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<RevokedAttestation, TAddress>> {
  const maybeAccount = await fetchMaybeRevokedAttestation(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRevokedAttestation<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<RevokedAttestation, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRevokedAttestation(maybeAccount);
}

export async function fetchAllRevokedAttestation(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<RevokedAttestation>[]> {
  const maybeAccounts = await fetchAllMaybeRevokedAttestation(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRevokedAttestation(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<RevokedAttestation>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeRevokedAttestation(maybeAccount),
  );
}

export function getRevokedAttestationSize(): number {
  return 98;
}
